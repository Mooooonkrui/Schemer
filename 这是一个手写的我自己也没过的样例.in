(letrec ([comp (lambda (x) (if (pair? x) (if (eq? (car x) (quote +)) (+ (comp (car (cdr x))) (comp (car (cdr (cdr x))))) (if (eq? (car x) (quote -)) (- (comp (car (cdr x))) (comp (car (cdr (cdr x))))) (if (eq? (car x) (quote *)) (* (comp (car (cdr x))) (comp (car (cdr (cdr x))))) x))) x))]) (comp (quote (+ (* 5 (- 4 2)) (- (+ 3 4) 3)))))
